import { EmbedSanityzePipe } from './embed-sanityze.pipe';

describe('EmbedSanityzePipe', () => {
  it('create an instance', () => {
    const pipe = new EmbedSanityzePipe();
    expect(pipe).toBeTruthy();
  });
});
